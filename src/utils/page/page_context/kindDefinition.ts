import KindModelPlugin from "main";
import { BasePageContext, KindDefinition } from "../../../types/PageContext";

export const kindDefinition = (
	plugin: KindModelPlugin, 
	base: BasePageContext
): KindDefinition => {
	
}
