import KindModelPlugin from "main";
import { BasePageContext, TypeDefinition } from "../../../types/PageContext";

export const typeDefinition = (plugin: KindModelPlugin, base: BasePageContext): TypeDefinition => {
	
}
