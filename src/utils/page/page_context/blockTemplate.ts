import KindModelPlugin from "main";
import { BlockTemplate } from "../../../types/BlockTemplate";
import { BasePageContext } from "types/PageContext";

export const blockTemplate = (plugin: KindModelPlugin, base: BasePageContext): BlockTemplate => {
	
}
