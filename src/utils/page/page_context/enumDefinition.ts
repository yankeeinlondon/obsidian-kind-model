import KindModelPlugin from "main";
import { BasePageContext, EnumDefinition } from "../../../types/PageContext";

export const enumDefinition = (plugin: KindModelPlugin, base: BasePageContext): EnumDefinition => {
	
}
